/**
 * Create a function that accepts an array 
 * and returns the last item in the array.
 * Examples :
 * getLastItem([1, 2, 3]) ➞ 3
 * getLastItem(["cat", "dog", "duck"]) ➞ "duck"
 * getLastItem([true, false, true]) ➞ true
 */

function getLastItem(array) {

    // return array[array.length - 1]; Good
    // return array.pop(); normal
    // return array.at(-1); Good
    // return array.splice(-1)[0]; poor
    
};
let array = [1, 2, 3];
// console.log(getLastItem(array));
// console.log(array);